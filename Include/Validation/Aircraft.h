#ifndef VALIDATION_AIRCRAFT_H
#define VALIDATION_AIRCRAFT_H

// Headers utilizados
#include "Config.h"

/**
 * @brief Verifica se uma string `representa um ano`.
 * @param string `String`.
 * @return `Booleano` que indica se a string `representa um ano`.
 */
Boolean validateYear (const char *string);

#endif